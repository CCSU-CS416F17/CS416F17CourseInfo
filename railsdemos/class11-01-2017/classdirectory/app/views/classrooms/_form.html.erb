<%= form_with(model: classroom, local: true) do |form| %>
  <% if classroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(classroom.errors.count, "error") %> prohibited this classroom from being saved:</h2>

      <ul>
      <% classroom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :teacher %>
    <%= form.text_field :teacher, id: :classroom_teacher %>
  </div>

  <div class="field">
    <%= form.label :grade %>
    <%= form.number_field :grade, id: :classroom_grade %>
  </div>

  <div class="field">
    <%= form.label :classid %>
    <%= form.text_field :classid, id: :classroom_classid %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
